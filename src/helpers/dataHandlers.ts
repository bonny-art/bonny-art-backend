// export const getPatternDataByLanguage = (pattern: any, language: string) => {
//   if (language === 'ru') {
//     return pattern.title_ru;
//   } else if (language === 'en') {
//     return pattern.title_en;
//   } else {
//     return pattern.title;
//   }
// };
